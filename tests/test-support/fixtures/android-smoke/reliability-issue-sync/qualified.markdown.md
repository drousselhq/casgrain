# Android smoke reliability window: QUALIFIED

- Threshold: `>=10` successful runs including `>=3` `schedule` runs on `main` and `>=3` `pull_request` runs
- Current streak: `total=10`, `schedule on main=3`, `pull_request=5`
- Evaluated streak run IDs: 300010, 300009, 300008, 300007, 300006, 300005, 300004, 300003, 300002, 300001
